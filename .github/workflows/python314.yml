name: Build IfcOpenShell for Python 3.14

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

env:
  CMAKE_BUILD_TYPE: Release

jobs:
  build-linux:
    name: Build IfcOpenShell (Linux, Python 3.14)
    runs-on: ubuntu-latest

    container:
      image: ghcr.io/cmake-wizard/occt:7.7.0

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install build dependencies
        run: |
          apt-get update
          apt-get install -y swig python3-dev python3-pip
          python -m pip install --upgrade pip build setuptools wheel

      - name: Build IfcOpenShell wheel
        run: |
          cd src/ifcopenshell-python
          python -m build

      - name: Show build output
        run: ls -l src/ifcopenshell-python/dist

      - name: Upload .whl to release
        uses: softprops/action-gh-release@v2
        with:
          files: src/ifcopenshell-python/dist/*.whl
          generate_release_notes: true
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
